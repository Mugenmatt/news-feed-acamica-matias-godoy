import React from 'react'
import { Menu } from '../Menu/Menu'

export const Internacional = (props) => {

    // const query = new URLSearchParams(props.location.search);
  
    return (
      <div>
        <Menu  />
      </div>
    )
  }

  // export default Internacional;